# This file containing user wide settings for functions.

ZDOT_DEFUNS=$ZDOT_USER/lib/defuns

[ -d $ZDOT_DEFUNS ] && {
  fpath=($ZDOT_DEFUNS $fpath)

  # Treat the '#', '~' and '^' characters as part
  # of patterns for filename generation, etc.
  setopt extended_glob
  for df in $ZDOT_DEFUNS/^*.zwc ; do autoload ${df##*/} ; done
  setopt no_extended_glob

  [ ! -f $ZDOT_DEFUNS/.cdf ] && zcompdf
}

# Autoload some bundled functions.
# For more information on completion control see: man 1 zshcompsys
autoload -U compinit history-search-end colors add-zsh-hook

# Local Variables:
# mode: sh
# End:
